// This is the configuration file for props.
// Start from 0 and increment every time. 0 is the default prop.
//
// Strings settings are required!
// If you choose to not set anything else, you can remove the lines and they will be assigned as the default settings provided with this configuration file.
"Props"
{
	"0"
	{
		// Accessibility
		"enabled"			"1" // When disabled, style will be unusable. -1 to also make the style not show in menus.

		// Strings
		"name"				"Small Box" // Prop name.
		"shortname"			"SB"
		"modelpath"			"models/props_junk/wood_crate001a.mdl" // Prop Model Path.
		"command"			"sb; sbox; smallbox" // Commands to register for style changing, leave empty for no command. use a semicolon as a delimiter - requires server restart.

		// Physics
		"massscale"			"1.0" // Mass scale setting, 1.0 for default.

		// Feature excluding
		"unranked"			"0" // Unranked style. No ranking points and no records.
		"noreplay"			"0" // Disable replay bot for this style. Automatically included for unranked styles.

		// Rankings
		"rankingmultiplier"	"1.0" // Rankings points multiplier. Don't use for unranked styles.

		// Special flags
		"specialstring"		"" // For modularity. Separated with semicolon. Built-in flags: "segments".
		"ordering"			"0" // Ordering in menus where styles appear. If this value is not present, style ID will be used instead.

	}

	"1"
	{
		"name"				"Medium Box"
		"shortname"			"MB"
		"modelpath"           		"models/props_junk/wood_crate002a.mdl"
		"command"			"mb; mbox; mediumbox"
		"specialstring"			""
	}
	
	"2"
	{
		"name"				"Pallet"
		"shortname"			"P"
		"modelpath"			"models/props_junk/wood_pallet001a.mdl"
		"command"			"pallet"
		"specialstring"			""
	}

	"3"
	{
		"name"				"Chair"
		"shortname"			"CH"
		"modelpath"			"models/props/de_inferno/chairantique.mdl"
		"command"			"ch; chair"
		"specialstring"			""
	}
	"4"
	{
		"name"				"Barrel"
		"shortname"			"BL"
		"modelpath"			"models\props\de_train\barrel.mdl"
		"command"			"bl; barrel"
		"specialstring"			""
	}
}
